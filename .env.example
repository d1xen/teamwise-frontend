# Teamwise Frontend — Environment Variables
#
# Dev:  VITE_API_URL is NOT needed — Vite proxy routes /api to localhost:8080
# Prod: Set VITE_API_URL in your hosting platform (Vercel, etc.)
#
# VITE_API_URL=https://teamwise-api-production.up.railway.app
