// Re-exports from centralized game config — kept for backward compatibility
export {
    GAME_IN_GAME_ROLES as IN_GAME_ROLES,
    IN_GAME_ROLE_LABELS,
    getAvailableInGameRoles,
} from "@/shared/config/gameConfig";
